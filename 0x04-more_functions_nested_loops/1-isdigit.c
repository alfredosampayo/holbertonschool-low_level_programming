#include "holberton.h"

/**
 * _isdigit - check c if it is a digit or not
 *
 * Return: 1 if c is a digit otherwise return 0
 * @c: integer for Betty
 */
int _isdigit(int c)
{

	if (c >= '0' && c <= '9')

	return (1);

	else

	return (0);
}


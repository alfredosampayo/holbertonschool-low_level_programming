#include "holberton.h"
/**
 * _isupper - check for upper or lower case
 *
 * Return: 1 if c is upper case, otherwise return 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);

	else

	return (0);
}

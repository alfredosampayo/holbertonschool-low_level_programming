#included "holberton.h"
/**
 * _array - reverses content of array
 * @a: an array of integers
 * @n: the number of elements to swap
 *
 * Return: nothing.
 */
 void reverse_array(int *a, int n)
{
	int i;

	int x;

	for (i = 0; a[i] != '\0'; i++)
	;
	for (x = 0; n[x] != '\0'; x++, i++);

	{
		a[i] = n[x]

	}

		a[i + 1] = '\0';

	return (0);

}

